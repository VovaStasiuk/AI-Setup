# Configure Project Example

This walkthrough shows the expected output shape for `/ai-setup-configure-project` after AI Setup initializes a repo.

The example is not a runnable project. It demonstrates how an agent should separate evidence from assumptions, use `.ai/profile.md` when present, mark commands as verified or candidate, and ask before writing project files.

Start with [configuration-plan.md](configuration-plan.md).
