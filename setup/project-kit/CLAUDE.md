# Claude Instructions

Mirror `AGENTS.md` for Claude Code. Project instructions override global skills.

Read `AGENTS.md` first. Use global `engineering-baseline` and `developer-orchestrator` only as workflow guidance; this project's files define the actual stack, commands, architecture, and design.
