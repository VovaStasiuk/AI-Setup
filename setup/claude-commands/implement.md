Use `engineering-baseline`, then `implementation-agent`.

Implement the requested task or approved plan. Inspect project instructions first, recommend the execution route if multiple major routes are available, keep edits scoped, verify with focused checks, and summarize results.
