Use `ai-setup-manager`.

Configure the current project after AI Setup init. Locate the installer if needed, run `./install.sh --audit-project <current-project>` when available, inspect project docs/configs/source layout/design sources, draft concrete updates for `.ai/project-context.md`, `.ai/agent-workflow.md`, `.ai/issue-tracker.md`, `.ai/domain.md`, `.ai/tech-stack.md`, `.ai/commands.md`, `.ai/DESIGN.md`, `.ai/styleguide.md`, and relevant styleguides, then ask before writing. Mark unverified commands as candidates and do not invent design direction.
