Use `ai-setup-manager`.

Audit the current project's AI setup. Locate the installer and run `./install.sh --audit-project <current-project>`. Summarize missing or weak files and recommend the next safe action.
