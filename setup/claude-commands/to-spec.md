Use `engineering-baseline`, then `to-spec`.

Turn the current conversation, clarified feature request, or approved plan into a decision-complete spec. Do not run a new interview unless a missing decision would materially change the spec. Use project domain language, capture implementation and testing decisions, and recommend `/to-tickets` or `/implement` as the next step.
