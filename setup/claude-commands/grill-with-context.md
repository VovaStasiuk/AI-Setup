Use `engineering-baseline`, then `grill-with-context`.

Pressure-test the idea before planning or implementation. Inspect relevant project docs/code when useful, ask one focused question at a time, recommend an answer with reasoning, clarify shared language, and end by asking whether to propose updates to project docs.
