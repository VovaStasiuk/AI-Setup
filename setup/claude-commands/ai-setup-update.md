Use `ai-setup-manager`.

Update or reinstall the global AI Setup. Locate the installer, run `./install.sh --dry-run --update`, explain what will change, ask before applying, then run `./install.sh --update`. Preserve existing project files.
