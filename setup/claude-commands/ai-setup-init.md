Use `ai-setup-manager`.

Initialize the current project with AI Setup. Locate the installer, run `./install.sh --dry-run --init-project <current-project>`, explain what will be added or skipped, ask before applying, then run the real project initialization if approved. After init, offer `/ai-setup-configure-project` to inspect the repo and draft useful `.ai` starter file updates.
